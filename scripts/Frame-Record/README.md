#### 说明

- 基于原项目略微做了调整，添加了多语言，摒弃了旧的单独引入json包的形式，使用aseprite后面版本提供的json api，但是对版本兼容需要提高，由23提升到25的兼容性。


#### 帧记录

- 可以记录用户在图层中绘制的操作并生成帧动画，每次操作为一帧动画。

#### 目录结构

json：json解析文件
config：配置文件，如访问密钥等
main：主函数
focusManager：监听管理器
